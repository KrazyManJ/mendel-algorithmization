/*
 * Příklad 3.1 - Vytvořte program v jazyce C++ pro výpočet aritmetického průměru tří čísel.
 */
#include <iostream>

using namespace std;

int main() {
    double long a, b, c;
    cin >> a >> b >> c;
    cout << (a+b+c/3) << endl;
    return 0;
}